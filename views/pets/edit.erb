<h1>Edit information for <%= @pet['name'] %></h1>

<form action="/pets/<%= @pet['id'] %>" method="post">
  <label for = "name-field">Name:</label>
  <input type="text" name="name" value="<%= @pet['name']%>">

  <label for="dob-field">Date of Birth:</label>
  <input type="text" name="dob" value="<%= @pet['dob'] %>">

  <label for="type-field">Breed:</label>
  <input type="text" name="type" value="<%= @pet['type'] %>">

  <label for="owner-selection">Owner:</label>
  <select name="owner_id">
    <% for owner in @owners %>
    <option value="<%= owner.id %>"><%= owner.name %></option>
    <% end %>
  </select>

 <label for="vet-selection">Vet:</label>
 <select name="vet_id" selected="<%=  @pet['vet_id'] %>">
   <% for vet in @vets %>
     <% if vet.id == @pet['vet_id'] %>
      <option value=<%= vet.id %>selected><%= vet.name %></option>
      <% else %>
      <option value=<%= vet.id %>><%= vet.name %></option>
     <% end %>
   <% end %>
  </select>

  <label for="notes-field">Notes:</label>
  <textarea name="notes" rows="8" cols="80"></textarea>

  <input type="submit" value="Register">
</form>
